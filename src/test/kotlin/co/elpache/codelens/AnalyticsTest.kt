package co.elpache.codelens;

import org.assertj.core.api.Assertions.assertThat
import org.junit.Test

class AnalyticsTest {

//  @Test
  fun `Can get graph view`() {



//    assertThat(toMap(ct))
//      .isEqualTo(
//        mapOf(
//          "rootVid" to vidA,
//          vidA to mapOf("data" to mapOf("name" to "a"), "parent" to null, "children" to listOf(vidB)),
//          vidB to mapOf("data" to mapOf("name" to "b"), "parent" to vidA, "children" to listOf(vidC)),
//          vidC to mapOf("data" to mapOf("name" to "c"), "parent" to vidB, "children" to listOf<String>())
//        )
//      )
  }

}
